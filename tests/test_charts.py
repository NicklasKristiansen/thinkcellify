import pytest
from thinkcellify._json_utils.charts import create_chart
from thinkcellify._json_utils._chart_utils import (
    _format_header,
    _generate_row_labels,
    _empty_row,
    _format_data_row,
    DEFAULT_ROW_LABELS
)

@pytest.mark.parametrize(
        "kwargs",

        [
            
        ]

)
def test_internal_functions(kwargs):
    pass


